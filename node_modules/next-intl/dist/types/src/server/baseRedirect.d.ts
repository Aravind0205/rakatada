import { redirect as nextRedirect } from 'next/navigation';
import { AllLocales, ParametersExceptFirst } from '../shared/types';
export default function baseRedirect(pathname: string, locale: AllLocales[number], ...args: ParametersExceptFirst<typeof nextRedirect>): never;
