'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var getIntl = require('../server/getIntl.js');
var useHook = require('./useHook.js');

let hasWarned = false;
function useIntl() {
  if (!hasWarned) {
    hasWarned = true;
    console.warn('`useIntl()` is deprecated and will be removed in the next major version. Please switch to `useFormatter()`.');
  }
  return useHook.default('useIntl', getIntl.default());
}

exports.default = useIntl;
