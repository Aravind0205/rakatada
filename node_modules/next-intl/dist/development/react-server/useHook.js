'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');

function useHook(hookName, promise) {
  try {
    return React.use(promise);
  } catch (error) {
    if (error instanceof TypeError && error.message.includes("Cannot read properties of null (reading 'use')")) {
      throw new Error("`".concat(hookName, "` is not callable within an async component. To resolve this, you can split your component into two, leaving the async code in the first one and moving the usage of `").concat(hookName, "` to the second one.\n\nExample:\n\nasync function Profile() {\n  const user = await getUser();\n  return <ProfileContent user={user} />;\n}\n\nfunction ProfileContent({user}) {\n  // Call `").concat(hookName, "` here and use the `user` prop\n  return ...;\n}") , {
        cause: error
      });
    } else {
      throw error;
    }
  }
}

exports.default = useHook;
