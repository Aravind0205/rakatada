'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var core = require('use-intl/core');
var getConfig = require('./getConfig.js');
var getLocaleFromHeader = require('./getLocaleFromHeader.js');

let hasWarned = false;

/** @deprecated Please switch to `getFormatter`. */
const getIntl = React.cache(async () => {
  if (!hasWarned) {
    hasWarned = true;
    console.warn("\n`getIntl()` is deprecated and will be removed in the next major version. Please switch to `getFormatter()`.\n\nLearn more: https://next-intl-docs.vercel.app/docs/environments/metadata-route-handlers\n");
  }
  const locale = getLocaleFromHeader.default();
  const config = await getConfig.default(locale);
  return core.createIntl(config);
});
var getIntl$1 = getIntl;

exports.default = getIntl$1;
