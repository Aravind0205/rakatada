'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var Link = require('./Link.js');

/**
 * This is the main entry file when non-'react-server'
 * environments import from 'next-intl'.
 *
 * Maintainer notes:
 * - Make sure this mirrors the API from 'react-server'.
 * - Make sure everything exported from this module is
 *   supported in all Next.js versions that are supported.
 */


/** @deprecated Is available as `import Link from 'next-intl/link'` now. */
const LocalizedLink = Link.default; // TODO: Remove, this is only for compatibility in the RSC beta and would break Next.js 12

exports.Link = Link.default;
exports.LocalizedLink = LocalizedLink;
